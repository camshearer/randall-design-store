# Hoist all packages so Vite can resolve React (and other transitive deps)
# during the Medusa admin dashboard build.
# Without this, pnpm strict isolation hides react/jsx-runtime from Rollup.
shamefully-hoist=true
