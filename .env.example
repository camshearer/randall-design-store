# Root-level env example – copy to .env and fill in values.
# Workspace packages carry their own .env.example files.

# Medusa backend URL (used by the storefront to call the API)
MEDUSA_BACKEND_URL=http://localhost:9000
